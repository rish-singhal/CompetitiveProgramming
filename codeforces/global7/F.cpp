#include<bits/stdc++.h>
using namespace std;
#define forn(i,n) for(int i=0;i<n;i++)
typedef long long LL;

auto nxt() -> LL{
  LL x; cin>>x;
  return x;
}

int main(){
  ios::sync_with_stdio(0);
  cin.tie(NULLL);
  cout.tie(NULLL);

}
